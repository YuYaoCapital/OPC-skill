---
name: fund-weekly-review
description: 生成单只基金的纯周度陪伴/回顾报告（不含横纵分析法深度评价）。融合产品基本信息、持仓周度表现、全球市场速览、热门主题、基金经理观点、本周业绩归因、后市展望与简要基金经理档案，输出5页以内、红色主调的Markdown/PDF报告。同时可输出HTML网页报告（风格与交互完全参考portfolio-week-companion）。适用于理财经理向客户发送持营基金定期陪伴内容。支持主动权益产品和量化产品的双框架分析。如需融合版（含横纵分析法深度档案），请使用 fund-weekly-hybrid。
---

# 持营基金周回顾

本 skill 用于生成单只基金的**纯周度陪伴/回顾报告**（不含横纵分析法深度评价）。报告融合产品概况、全球市场速览、热门主题、基金经理观点、本周业绩表现与归因、后市展望以及简要的基金经理档案，输出为红色主调的 Markdown 或 PDF 报告（5页以内）。

> 如需**融合版**（同时包含横纵分析法深度评价的基金经理档案），请使用 `fund-weekly-hybrid` skill。

## 触发条件

用户以下述方式请求时触发：
- "生成 XX 基金的周度回顾"
- "帮我写一份 XX 基金周度陪伴"
- "持营基金周报告"
- "基金周度回顾"
- "本周基金表现分析"
- "基金周报"
- "生成 HTML 周度回顾" / "网页版基金周报"
- "pages.dev 基金报告" / "在线基金周报"

**注意**：当用户明确提到"融合版""横纵分析""深度周度""产品分析+周度回顾""既分析产品又回顾本周"时，应触发 `fund-weekly-hybrid`（融合版），而非本 skill。

## 输入变量

执行前必须明确以下变量：

```
基金代码 = 用户提供的6位基金代码（如 007119）
基金名称 = 用户提供的基金全称或简称
基金经理姓名 = 当前管理该基金的核心基金经理
报告日期 = 上周最后一个交易日日期（如 2026-07-03）
产品类型 = 主动权益 / 量化（默认主动权益，如基金经理有量化背景或产品名称含"量化""指数增强""多因子"等则判定为量化）
评价基准日 = 默认当前日期
```

## 产品类型识别

生成报告前，先判断产品属于**主动权益型**还是**量化型**，后续章节根据类型切换分析框架：

| 判断维度 | 主动权益型 | 量化型 |
|---|---|---|
| 基金经理背景 | 传统研究/投资背景，擅长个股研究、产业判断 | 数学/统计/金融工程背景，擅长模型开发、因子挖掘 |
| 产品名称特征 | 无"量化""指数增强""多因子"等字样 | 含"量化""指数增强""多因子""AI策略"等字样 |
| 持仓特征 | 前十大重仓占比较高（通常>40%） | 前十大重仓占比极低（通常<10%），持仓极度分散 |
| 策略特征 | 自上而下/自下而上选股，行业配置+个股精选 | 多因子模型驱动，统计套利，量化择时/选股 |
| 换手特征 | 中低换手率（通常<200%） | 中高换手率（通常200%-500%+） |
| 业绩归因 | 个股贡献、行业配置、集中度效应 | 因子暴露、模型有效性、跟踪误差、风格漂移 |

**判定规则**：若产品名称含"量化""指数增强""多因子""AI策略"等关键词，或基金经理有数学/统计/金融工程博士背景，或前十大重仓合计<15%，则判定为**量化型**。其余判定为**主动权益型**。

## 报告结构（从上到下，12个模块）

**标题格式规范**：每个模块标题使用中文数字前缀（一、二、三...），不使用emoji图标。如：一、产品概况；二、核心持仓上周表现；三、产品表现。子模块用阿拉伯数字+小数点，如：3.1 成立至今净值走势、3.2 年度业绩、3.3 历史回撤修复能力。

> **模块编号与PDF固定模板对齐**：PDF生成器 `fund_weekly_pdf.py` 中模块顺序已硬编码，数据注入时需确保字段名称与以下模块对应。
>
> **样式固化声明**：PDF模板 `fund_weekly_pdf.py` 的样式已完全固化，所有颜色、字号、边距、间距常量均硬编码在脚本顶部。LLM执行时**不得修改**模板中的任何样式参数，仅通过修改 `funds_data.json` 数据文件来更新报告内容。数据计算逻辑（如`colored_pct`正红负绿、`gaussian_smooth` sigma=4）已固化。`overview`标签映射已固化：估算净值→近一周收益、估算涨跌幅→近一年收益、近1年收益→机构持仓占比。顶部4列表格列宽已固化：`[35mm, 48mm, 35mm, 48mm]`。
>
> **HTML模块差异**：HTML报告在PDF的12个模块基础上，在「十、后市怎么看？」之后额外增加了三个企微专属模块（十一、您该怎么做？；十二、财经新闻与沟通策略；十三、理财经理本周沟通tips），并将「基金经理与产品档案」顺延为第十四模块。PDF中不包含这三个模块。

### 1. 封面（含4指标表）

格式：
```
# 基金名称 周度陪伴报告

报告日期 {period_start} 至 {period_end} | 制作日期 {report_date}
```

要求：
- 封面含4指标表：最新净值 | 近一周 | 今年以来 | 成立以来
- 顶部4列表格列宽已固化：`[35mm, 48mm, 35mm, 48mm]`
- 红色主调（PDF 渲染时标题和分隔线使用红色）

---

### 2. 一、产品概况（含9项指标+产品定位）

**基本信息表**（主动权益与量化通用，共9项）

| 项目 | 内容 |
|---|---|
| 产品代码 | 基金代码（A类/C类） |
| 基金经理 | 核心基金经理姓名 |
| 最新净值 | 最新净值（净值日期） |
| 日涨跌 | 最新日涨跌幅 |
| 上周收益 | 上周累计涨跌幅 |
| 今年以来 | 今年以来收益 |
| 成立以来 | 成立以来总回报 |
| 基金类型 | 如：混合型-偏股 / 混合型-灵活 |
| 最新规模 | 最新资产规模 |

**产品定位**（`positioning`）：不少于80字，说明产品策略定位、目标客群、核心特征

**成立至今净值走势简述**：1-2句话概括成立以来净值走势特征



### 3. 二、核心持仓上周表现（含持仓表+持仓观察）

#### 3.1 主动权益型

| 股票 | 行业 | 占净值 | 上周涨跌 | 说明 |
|---|---|---|---|---|
| 股票名称 | 所属行业 | 占净值比例 | 上周涨跌幅 | 1-2句核心逻辑说明 |

要求：
- 列出前十大重仓股（每只股票占净值、上周涨跌、1-2句说明）
- 表格字体9pt、居中、行高紧凑
- 列宽根据内容自适应（说明列最宽，行业/涨跌列较窄）
- 底部标注：`持仓数据截至{holding_source}｜净值数据截至{data_cutoff}`

**持仓观察**（`holding_note`）：不少于100字，对整体持仓结构、行业分布、调仓线索的观察

---

#### 3.2 量化型：核心因子与行业配置上周表现

**因子暴露表**

| 因子维度 | 暴露方向 | 上周表现 | 对净值影响 | 说明 |
|---|---|---|---|---|
| 价值因子 | 正向/负向/中性 | 因子收益 | 正向/负向 | 低估值/高估值风格上周表现 |
| 成长因子 | 正向/负向/中性 | 因子收益 | 正向/负向 | 高成长/低成长风格上周表现 |
| 动量因子 | 正向/负向/中性 | 因子收益 | 正向/负向 | 强势股/反转股上周表现 |
| 质量因子 | 正向/负向/中性 | 因子收益 | 正向/负向 | 高盈利/低盈利风格上周表现 |
| 市值因子 | 大盘/小盘/中性 | 因子收益 | 正向/负向 | 大小盘风格上周表现 |
| 行业偏离 | 超配/低配行业 | 行业涨跌 | 正向/负向 | 模型超配行业上周表现 |

要求：
- 不列出个股（量化产品持仓极度分散，个股意义不大），改为分析**因子暴露**和**行业配置偏离**
- 结合基金的实际因子暴露方向（如模型偏好小盘成长、价值质量等），说明上周哪些因子有效/失效
- 若因子数据无法获取，可用重仓行业替代，列出前5大行业配置的上周表现

---

### 4. 三、产品表现（3.1净值走势、3.2年度业绩、3.3历史回撤修复能力）

#### 4.1 成立至今净值走势
- 用 reportlab 的 Drawing + LinePlot 绘制折线图（基金净值蓝色实线 + 业绩比较基准灰色虚线）
- 横轴：时间轴（年度），纵轴：累计收益率
- 图例清晰标注两条线
- 图表下方附净值走势简述（`nav_summary`，不少于150字）

#### 4.2 年度业绩表

| 年度 | 基金收益 | 业绩比较基准 | 超额收益 |
|---|---|---|---|
| 成立当年 | +X.XX% | +X.XX% | +X.XX% |
| 次年 | +X.XX% | +X.XX% | +X.XX% |
| ... | ... | ... | ... |
| 今年以来 | +X.XX% | +X.XX% | +X.XX% |
| **成立以来** | **+X.XX%** | **+X.XX%** | **+X.XX%** |

**业绩比较基准说明**：如"沪深300指数收益率×55% + 中证港股通综合指数(人民币)收益率×35% + 中证债券指数收益率×5% + 银行活期存款利率×5%"

要求：
- 列出从成立年度到当前年度的年度收益
- 最后一行汇总成立以来总收益
- 格式紧凑、字体9pt、居中

#### 4.3 历史回撤修复能力

简要说明基金历史最大回撤的发生时间、幅度、当前修复状态（`drawdown_repair_summary`，与3.3对应，不少于150字）

---

### 5. 四、基金经理观点

#### 5.1 主动权益型基金经理观点

**内容要求**：
- 至少4段话，每段150字以上
- 引用最新季报/年报中基金经理的"投资策略和运作分析"原文（直接引用，用引号标注）
- 如有公开访谈/路演，引用核心判断
- 阐述基金经理当前的投资策略方向（增配什么、减持什么、关注什么）
- 体现基金经理的底层投资框架和近期调仓逻辑
- **投资理念补充**：阐述基金经理的底层投资哲学（如"实业思维+精选个股+长期持有+高仓位运作"），以及当前投资理念与市场环境的契合度

---

**量化型基金经理观点**：
- 引用基金经理关于量化策略的阐述（如模型迭代方向、因子优化、数据挖掘等）
- 说明当前模型对市场风格的适应度（如近期价值因子失效、动量因子有效等）
- 阐述近期策略调整方向（如新增因子、优化权重、调整行业偏离等）
- 体现量化投资的底层逻辑（如统计套利、多因子模型、机器学习等）
- 若基金经理有公开访谈，引用其对量化策略迭代的观点
- **投资理念补充**：阐述基金经理的量化投资哲学（如"多因子模型+统计套利+严格风控"），以及当前策略与市场环境的契合度

---

### 6. 五、上周产品表现（阶段收益对比+表现点评）

**阶段收益对比表**

| 时间维度 | 基金收益率 | 基准收益率 | 超额收益 |
|---|---|---|---|
| 上周 | +X.XX% | +X.XX% | +X.XX% |
| 近1月 | +X.XX% | +X.XX% | +X.XX% |
| 近3月 | +X.XX% | +X.XX% | +X.XX% |
| 近6月 | +X.XX% | +X.XX% | +X.XX% |
| 近1年 | +X.XX% | +X.XX% | +X.XX% |
| 成立以来 | +X.XX% | +X.XX% | +X.XX% |

**表现点评**（`weekly_comment`）：不少于80字，概括上周整体表现特征

要求：
- 6个时间段均显示基金收益率、基准收益率、超额收益（如数据缺失可置空，但保留列结构）

---

### 7. 六、回撤修复数据

| 回撤事件 | 最大回撤 | 修复状态 | 修复时间 |
|---|---|---|---|
| 历史回撤事件名称 | 最大回撤幅度 | 已修复/修复中 | 修复所需天数 |

**回撤修复详细说明**（`drawdown_summary`，不少于150字，与`drawdown_records`对应）：
- 说明基金历史最大回撤的发生时间、幅度、当前修复状态
- 列出3-5个历史重要回撤事件及修复时间
- 当前波动作为最后一行
- 最大回撤说明需包含具体数据和时间节点

---

### 8. 七、全球市场速览（含A股估值指标+市场点评）

| 市场 | 指数 | 上周收盘 | 上周涨跌 | 关键事件 |
|---|---|---|---|---|
| A股 | 沪深300 | 收盘点位 | 涨跌幅 | 上周最重要的1-2个事件 |
| A股 | 上证指数 | 收盘点位 | 涨跌幅 | 关键事件 |
| A股 | 创业板指 | 收盘点位 | 涨跌幅 | 关键事件 |
| A股 | 科创50 | 收盘点位 | 涨跌幅 | 关键事件 |
| 港股 | 恒生指数 | 收盘点位 | 涨跌幅 | 关键事件 |
| 港股 | 恒生科技 | 收盘点位 | 涨跌幅 | 关键事件 |
| 美股 | 标普500 | 收盘点位 | 涨跌幅 | 关键事件 |
| 美股 | 纳斯达克 | 收盘点位 | 涨跌幅 | 关键事件 |
| 亚太 | 日经225 | 收盘点位 | 涨跌幅 | 关键事件 |
| 商品 | COMEX黄金 | 收盘点位 | 涨跌幅 | 关键事件 |
| 商品 | WTI原油 | 收盘点位 | 涨跌幅 | 关键事件 |
| 外汇 | 美元指数 | 收盘点位 | 涨跌幅 | 关键事件 |

**A股估值指标**

| 指标 | 数值 | 估值分位 | 评价 |
|---|---|---|---|
| 沪深300 PE | 数值 | 分位% | 如：中等偏上 |
| 创业板指 PE | 数值 | 分位% | 如：偏高 |
| 科创50 PE | 数值 | 分位% | 如：高 |
| 10Y国债收益率 | 数值 | 历史位置 | 如：历史低位 |
| 股债性价比 | 数值 | 水平 | 如：中等 |

**市场点评**（`market_comment`）：不少于50字

**上周关键词**：用3-5个关键词概括上周市场特征（如：AI算力高波动 · 高弹性双刃剑 · 极致分化）

---

### 9. 八、热门主题表现

| 主题 | 代表ETF | 上周涨跌 | 核心驱动 |
|---|---|---|---|
| 主题名称 | ETF名称 | 涨跌幅 | 1句核心驱动逻辑 |

**热门主题描述**（`theme_comment`）：不少于100字

要求：
- 列出10个左右热门主题
- 覆盖本基金重仓相关主题（如光模块、PCB、半导体、新能源等）
- 以及市场关注度高的主题（如白酒、医药、中概互联等）

---

### 10. 九、为什么上周会有波动？（归因分析）

**内容要求**：
- 4条归因分析，每条至少150字
- 正向因素编号用红色；负向因素编号用绿色
- 分维度分析上周波动来源

**主动权益型归因维度**：

1. **核心持仓端**：本基金重仓板块的上周表现（如AI算力、新能源、消费等），具体个股涨跌分析
2. **港股/海外端**：港股配置的表现，对净值的影响
3. **稳定器/拖累端**：组合中表现平稳或拖累的板块
4. **结构放大端**：高集中度/高仓位结构对波动的放大效应

每部分结尾用1-2句话小结核心原因。

---

#### 10.2 量化型归因分析

分维度分析上周波动来源，通常包括：

1. **因子暴露端**：本基金暴露的因子（价值/成长/动量/质量/市值等）上周哪些有效、哪些失效，对净值的影响
2. **行业偏离端**：模型超配/低配的行业上周表现，对净值的影响
3. **模型适配端**：当前市场环境下，量化模型的适应性如何
4. **结构放大端**：高仓位/高换手率结构对波动的放大或平滑效应

每部分结尾用1-2句话小结核心原因。

---

### 11. 十、后市怎么看？（后市展望）

**内容要求**：
- 4条展望，每条至少150字
- 正向因素编号用红色；负向因素编号用绿色
- 分维度展望后市

#### 11.1 主动权益型展望

分维度展望后市，通常包括：

1. **核心持仓产业链**：长期景气度判断+短期风险点
2. **港股市场**：估值空间+流动性预期
3. **其他重要板块**：如新能源、消费、医药等
4. **宏观环境**：利率、通胀、政策等

---

#### 11.2 量化型展望

分维度展望后市，通常包括：

1. **因子有效性展望**：当前市场环境下哪些因子可能持续有效
2. **行业配置展望**：模型可能超配/低配的方向
3. **模型适配性**：后续市场环境对量化策略的友好度
4. **宏观环境**：利率、流动性、政策对量化策略的影响

---

### 12. 十一、基金经理与产品档案

#### 12.1 主动权益型基金经理档案

**基金经理基本信息表**

| 项目 | 内容 |
|---|---|
| 姓名 | |
| 出生年份 | |
| 学历 | |
| 证券从业年限 | |
| 公募管理年限 | |
| 现任职务 | |
| 管理规模 | |
| 代表业绩 | |

- 职业履历简述（关键节点：从研究到投资的路径，关键岗位变动）
- 投资风格定位（一句话概括：如"精选个股+长期持有+高仓运作的宗师级成长价值型基金经理"）

**共管基金经理**：如有，简要介绍

**产品核心特征表**

| 维度 | 特征 |
|---|---|
| 核心策略 | |
| 股票仓位 | |
| 持股集中度 | |
| 行业集中度 | |
| 港股配置 | |
| 换手率 | |
| 核心能力 | |
| 主要风险 | |

---

#### 12.2 量化型基金经理档案

**基金经理基本信息表**

| 项目 | 内容 |
|---|---|
| 姓名 | |
| 出生年份 | |
| 学历 | |
| 证券从业年限 | |
| 公募管理年限 | |
| 现任职务 | |
| 管理规模 | |
| 代表业绩 | |

- 量化背景简述（数学/统计/金融工程背景，模型开发经历，关键岗位）
- 投资风格定位（一句话概括：如"多因子模型驱动的量化均衡型基金经理，擅长统计套利与因子挖掘"）

**共管基金经理**：如有，简要介绍

**产品核心特征表**

| 维度 | 特征 |
|---|---|
| 核心策略 | |
| 股票仓位 | |
| 持股集中度 | |
| 行业集中度 | |
| 港股配置 | |
| 换手率 | |
| 核心能力 | |
| 主要风险 | |

---

### 13. 十二、交互式HTML报告

在PDF末尾附上一页「HTML在线报告」，包含二维码和访问链接 `https://fundadvisor.pages.dev/reports/基金简称_周度回顾.html`

---

### 14. Footer风险提示（独立模块）

- 风险提示：本报告仅供参考，不构成投资建议。基金过往业绩不预示未来表现，投资有风险，入市需谨慎。
- Footer不含数据来源和报告日期（这些已在封面顶部显示）
- 数据截止统一使用 `data_cutoff` 字段

## 写作风格要求

1. **简洁有力**：每段不超过200字，避免冗长论述
2. **数据驱动**：每个判断都有具体数据支撑（净值、涨跌幅、占比、PE、因子收益等）
3. **专业但不晦涩**：用投资者能理解的语言解释专业判断（量化术语需适当解释）
4. **红色主调 + 红涨绿跌**：
   - PDF 渲染时标题、分隔线、表格头部使用红色系（#8b0a1a / #c41e3a）
   - **数据颜色规则**：涨跌幅/收益率等百分比数据，正数用红色（#e74c3c / #dc2626），负数用绿色（#27ae60 / #16a34a）；净值变化同理，大于0红色，小于0绿色
   - **归因分析编号颜色**：正向因素（如核心持仓强势、稳定器贡献）编号用红色；负向因素（如拖累端、回调板块）编号用绿色
5. **紧凑排版**：页边距1.5cm，正文字号9pt，表格字号9pt，行距紧凑，目标5页以内
6. **标题格式**：PDF和HTML中每个模块标题用中文数字一、二、三...前缀，不使用emoji图标。子模块用阿拉伯数字+小数点（如：3.1、3.2、3.3）
7. **类型适配**：主动权益侧重量化个股分析，量化侧重量化因子分析，不混淆
7. **HTML兼容**：HTML报告样式完全参考融合报告（排除横纵分析深度档案部分），使用红色主调（#8b0a1a / #c41e3a），与PDF视觉统一。卡片式布局、sticky导航栏、一键复制功能。确保PDF与HTML两种输出格式视觉统一

## 输出格式（样式锁定，调用固定模板）

> **重要**：PDF 和 HTML 的生成**不再由 LLM 从零写代码**，而是通过调用**固定模板脚本** + 注入 JSON 数据实现。这确保了每次生成的报告样式**完全一致**，仅数据不同。
> 
> **样式锁定声明**：本 Skill 的所有视觉样式参数均硬编码在模板脚本中。LLM 执行时**绝对不得修改**任何颜色、字号、边距、间距定义。仅通过修改 `funds_data.json` 数据文件来更新报告内容。如需调整样式，必须直接修改模板源文件本身，并在本 SKILL.md 中同步更新版本号。

### 模板文件位置

| 文件 | 路径 | 说明 |
|------|------|------|
| **PDF 生成器** | `D:\OPC-skill\skills\fund-weekly-review\templates\fund_weekly_pdf.py` | reportlab 固定脚本，样式全部硬编码 |
| **HTML 生成器** | `D:\OPC-skill\skills\fund-weekly-review\templates\generate_htmls.py` | HTML 固定模板，CSS 全内联，样式全部硬编码 |
| **数据文件（输入）** | `D:\OPC-skill\skills\portfolio-week-companion\site\reports\funds_data.json` | JSON 数据注入，每次生成前更新此文件 |
| **HTML 首页** | `D:\OPC-skill\skills\portfolio-week-companion\site\index.html` | 报告列表页，跳转逻辑已锁定 |

### 执行流程

```
1. 收集基金数据（净值、持仓、市场、观点等）
2. 将数据写入 funds_data.json
3. 调用 python fund_weekly_pdf.py  funds_data.json  output.pdf
4. 调用 python generate_htmls.py 生成 HTML（自动从 funds_data.json 读取）
5. 生成 PDF + HTML 报告（样式完全一致，仅数据不同）
6. git push 到 GitHub，Cloudflare Pages 自动部署
```

---

### PDF 模板 — `fund_weekly_pdf.py`（样式完全固化）

**样式常量（顶部硬编码，绝对锁定）**：

| 常量 | 值 | 说明 |
|------|-----|------|
| `FONT_PATH` | `C:/Windows/Fonts/simhei.ttf` | 黑体字体 |
| `C_PRIMARY` | `#8b0a1a` | 主标题红 |
| `C_PRIMARY_L` | `#c41e3a` | 二级标题红 |
| `C_UP` | `#dc2626` | 上涨红 |
| `C_DOWN` | `#16a34a` | 下跌绿 |
| `C_BG_LIGHT` | `#fef2f2` | 表头浅红底 |
| `FS_TITLE` | `18` | 主标题字号 |
| `FS_BODY` | `9` | 正文字号 |
| `FS_TABLE` | `9` | 表格字号 |
| `MARGIN_LEFT` | `25mm` | 左边距 |
| `MARGIN_RIGHT` | `20mm` | 右边距 |
| `MARGIN_TOP` | `20mm` | 上边距 |
| `MARGIN_BOTTOM` | `15mm` | 下边距 |

**模块构建顺序（`generate_report` 函数中硬编码）**：

```
封面（4指标表：最新净值|近一周|今年以来|成立以来）
  ↓
一、产品概况（overview 9项 + 产品定位）
  ↓
二、核心持仓上周表现（持仓表 + 持仓观察）
  ↓
三、产品表现
  ├── 3.1 成立至今净值走势（Chart 折线图）
  ├── 3.2 年度业绩（annual_returns 字典格式）
  └── 3.3 历史回撤修复能力（drawdown_records）
  ↓
四、基金经理观点（manager_views 列表）
  ↓
五、上周产品表现（weekly_performance 阶段对比表 + 表现点评）
  ↓
六、回撤修复数据（drawdown_repair_summary）
  ↓
七、全球市场速览（global_market + valuation 估值指标）
  ↓
八、热门主题表现（hot_themes + 主题描述）
  ↓
九、为什么上周会有波动？（attributions 归因分析）
  ↓
十、后市怎么看？（outlooks 后市展望）
  ↓
十一、基金经理与产品档案（profile + manager_bio）
  ↓
十二、交互式HTML报告（附录页，含二维码和访问链接）
  ↓
Footer 风险提示
```

**关键固化规则**：
- `colored_pct` 函数：正红负绿，已固化
- `gaussian_smooth` 函数：sigma=4，已固化
- `overview` 标签映射：估算净值→近一周收益、估算涨跌幅→近一年收益、近1年收益→机构持仓占比，已固化
- 封面4列表格列宽：`[35mm, 48mm, 35mm, 48mm]`，已固化
- 净值走势图：基金净值蓝色实线 + 基准灰色虚线，已固化
- 表格样式工厂：`make_table_style` / `make_table_style_centered`，已固化

---

### HTML 模板 — `generate_htmls.py`（样式完全固化）

**CSS 变量（硬编码在 `<style>` 中）**：

| 变量 | 值 | 说明 |
|------|-----|------|
| `--primary` | `#8b0a1a` | 主色调 |
| `--primary-light` | `#c41e3a` | 浅红 |
| `--up` | `#dc2626` | 上涨红 |
| `--down` | `#16a34a` | 下跌绿 |
| `--nav-h` | `52px` | 导航栏高度 |

**模块结构（14个 section，sec1 ~ sec14）**：

| sec | 模块名称 | 对应PDF模块 |
|-----|---------|------------|
| sec1 | 一、产品概况 | ✓ 一致 |
| sec2 | 二、核心持仓上周表现 | ✓ 一致 |
| sec3 | 三、产品表现 | ✓ 一致 |
| sec4 | 四、全球市场速览 | PDF中为「七」 |
| sec5 | 五、热门主题表现 | PDF中为「八」 |
| sec6 | 六、基金经理观点 | PDF中为「四」 |
| sec7 | 七、上周产品表现 | PDF中为「五」 |
| sec8 | 八、回撤修复数据 | PDF中为「六」 |
| sec9 | 九、为什么上周会有波动？ | ✓ 一致 |
| sec10 | 十、后市怎么看？ | ✓ 一致 |
| sec11 | 十一、您该怎么做？ | HTML独有 |
| sec12 | 十二、财经新闻与沟通策略 | HTML独有 |
| sec13 | 十三、理财经理本周沟通tips | HTML独有 |
| sec14 | 十四、基金经理与产品档案 | PDF中为「十一」 |

> **注**：HTML 在「十、后市怎么看？」之后增加了三个企微专属模块（十一~十三），PDF 中不包含。PDF 的「十二、交互式HTML报告」附录页在 HTML 中不出现。

**差异化下载策略（已固化在模板底部）**：

```javascript
(function() {
    var isMobile = /iPhone|iPad|iPod|Android|webOS|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent);
    if (!isMobile) {
        var pdfLinks = document.querySelectorAll('a[href$=".pdf"]');
        for (var i = 0; i < pdfLinks.length; i++) {
            pdfLinks[i].setAttribute('download', '');
        }
    }
})();
```

| 设备 | 点击「下载PDF」行为 | 原因 |
|------|-------------------|------|
| **PC端** | 直接下载 PDF 到本地 | JS 自动添加 `download` 属性 |
| **手机企微** | 打开 PDF 在线预览 | 不添加 `download` 属性，避免企微拦截 |

**导航栏跳转逻辑（首页 index.html 已固化）**：
- 企微移动端：`window.location.href = a.href`（当前页跳转）
- PC端：`target="_blank"`（新标签页打开）
- 原因：企微内置浏览器拦截 `window.open()`

---

### 数据文件结构（`funds_data.json`）

JSON 必须为**字典格式**，顶层 key 为基金代码（如 `"007119"`），值为基金数据字典。生成器会自动遍历所有基金生成对应报告。

**必填字段**：

```json
{
  "007119": {
    "code": "007119",
    "name": "睿远成长价值混合A",
    "type": "混合型-偏股",
    "manager": "傅鹏博、朱璘",
    "nav": 2.5020,
    "nav_date": "2026-07-10",
    "nav_history": [{"date": "2019-03-26", "nav": 1.0000}, ...],
    "weekly_return": -3.22,
    "ytd_return": 18.56,
    "daily_change": -3.95,
    "scale": "165.00亿",
    "benchmark": "业绩基准说明",
    "data_cutoff": "2026-07-10",
    "positioning": "产品定位说明（≥80字）",
    "holdings": [{"name": "宁德时代", "industry": "电力设备", "weight": "8.5%", "change": -5.2, "desc": "核心逻辑"}, ...],
    "holding_source": "2026年一季报",
    "holding_note": "持仓观察（≥100字）",
    "nav_summary": "净值走势简述（≥150字）",
    "annual_returns": {"2019": {"fund_return": 2.35, "benchmark_return": 4.12, "excess_return": -1.77}, ...},
    "drawdown_records": [{"event": "...", "drawdown": -23.5, "status": "已修复", "repair_time": "..."}, ...],
    "drawdown_summary": "回撤详细说明（≥150字）",
    "drawdown_repair_summary": "回撤修复数据模块内容",
    "manager_views": [{"title": "投资策略", "content": "..."}, ...],
    "weekly_performance": {"periods": [{"period": "近一周", "fund_return": -3.22, "benchmark_return": -0.79, "excess_return": -2.43}, ...]},
    "weekly_comment": "表现点评（≥80字）",
    "global_market": [{"market": "A股", "index": "沪深300", "close": "4780.79", "change": -1.96}, ...],
    "valuation": [{"label": "沪深300 PE", "value": "12.5", "percentile": "45%", "evaluation": "中等"}, ...],
    "market_comment": "市场点评（≥50字）",
    "hot_themes": [{"name": "AI算力/光模块", "etf": "AI算力ETF", "change": -12.8}, ...],
    "theme_comment": "热门主题描述（≥100字）",
    "attributions": [{"title": "核心持仓端", "content": "...", "positive": true}, ...],
    "outlooks": [{"title": "短期", "content": "...", "positive": true}, ...],
    "profile": {"info": {"基金经理": "傅鹏博、朱璘", "从业年限": "30年", ...}},
    "manager_bio": "基金经理简介"
  }
}
```

**数据格式规范**：
- `nav`: float，最新净值
- `weekly_return` / `ytd_return` / `daily_change`: float，百分比值（如 `-3.22` 表示 -3.22%）
- `annual_returns`: 每年为 `{"fund_return": X, "benchmark_return": X, "excess_return": X}` 字典（非字符串）
- `drawdown_records`: 每条含 `{"event": "...", "drawdown": X, "status": "...", "repair_time": "..."}`
- `attributions` / `outlooks`: 每条含 `{"title": "...", "content": "...", "positive": true/false}`，`positive` 控制编号颜色（true=红，false=绿）
- `global_market`: 每条含 `{"market": "...", "index": "...", "close": "...", "change": X}`，`change` 为 float
- `hot_themes`: 每条含 `{"name": "...", "etf": "...", "change": X}`，`change` 为 float，`etf` 可选
- `holdings`: 每条含 `{"name": "...", "industry": "...", "weight": "...", "change": X, "desc": "..."}`，`change` 为 float

**HTML 模板专属数据字段**：

除 PDF 共用字段外，`generate_htmls.py` 模板额外需要以下数据驱动的 HTML 字段（**禁止在模板中硬编码基金专属文案**）：

| 字段 | 类型 | 注入模块 | 说明 |
|------|------|---------|------|
| `manager_views` | string (HTML) | sec6 基金经理观点 | 含投资策略/理念/行业观点/风险提示4段，每段 `<p>` 标签包裹 |
| `weekly_analysis` | string (HTML) | sec7 上周分析 | 含市场概括/产品影响/投资建议3段，每段 `.highlight-box` div |
| `attributions` | string (HTML) | sec9 归因分析 | 正向/负向双栏 `.attr-box`，含具体持仓归因（如"东山精密等持仓受风格切换影响"） |
| `outlooks` | string (HTML) | sec10 后市展望 | 短/中/长期3个 `.outlook-item` |
| `advice` | string (HTML) | sec11 建议表 | 客户状态建议表 + 引导语 |
| `news` | string (HTML) | sec12 新闻沟通 | A股/美股/港股/黄金4个 `.highlight-box` |
| `comm_tips` | string (HTML) | sec13 沟通话术 | 定调/归因/建议 + 客群话术模板 + 常见异议回应 |

> **重要**：上述 7 个字段必须按基金实际持仓和基金经理生成，**严禁在模板中硬编码某只基金的专属内容**（如"傅鹏博/朱璘/立讯精密/宁德时代"等）。`generate_htmls.py` 的 `HTML_TEMPLATE` 中这些位置应使用 `{manager_views}`、`{weekly_analysis}` 等占位符，由数据文件注入。
- `nav`: float，最新净值
- `weekly_return` / `ytd_return` / `daily_change`: float，百分比值（如 `-3.22` 表示 -3.22%）
- `annual_returns`: 每年为 `{"fund_return": X, "benchmark_return": X, "excess_return": X}` 字典（非字符串）
- `drawdown_records`: 每条含 `{"event": "...", "drawdown": X, "status": "...", "repair_time": "..."}`
- `attributions` / `outlooks`: 每条含 `{"title": "...", "content": "...", "positive": true/false}`，`positive` 控制编号颜色（true=红，false=绿）
- `global_market`: 每条含 `{"market": "...", "index": "...", "close": "...", "change": X}`，`change` 为 float
- `hot_themes`: 每条含 `{"name": "...", "etf": "...", "change": X}`，`change` 为 float，`etf` 可选
- `holdings`: 每条含 `{"name": "...", "industry": "...", "weight": "...", "change": X, "desc": "..."}`，`change` 为 float

---

### 1. Markdown（内容初稿，可选）

先生成 Markdown 文件（.md），作为内容校对基础。但**正式交付以 PDF + HTML 为准**。

### 2. PDF（红色主调，正式存档，5页以内）
**标准交付：每次同时生成 PDF + HTML 两种格式**。PDF 作为正式存档/分发文档，**调用固定模板脚本生成**（不是从零写代码）：
- **生成方式**：调用 `D:\OPC-skill\skills\fund-weekly-review\templates\fund_weekly_pdf.py`，传入 JSON 数据文件
- **中文字体**：使用 Windows 系统字体 `C:/Windows/Fonts/simhei.ttf`（黑体）
- **红色主调**：标题 `#8b0a1a`、二级标题 `#c41e3a`、表格表头红色底白字、页面底部红色分隔线
- **红涨绿跌**：涨跌幅/收益率等百分比数据，正数红色（`#dc2626`），负数绿色（`#16a34a`）；净值变化大于0红色，小于0绿色
- **归因编号颜色**：正向因素编号红色；负向因素编号绿色
- **信息密度**：5页以内，表格紧凑、字体9pt、行高12-14pt、列宽自适应（内容多的列更宽）
- **页面边距**：左25mm、右20mm、上20mm、下15mm
- **结构化模块**：封面（含4指标表）、产品概况（9项指标+产品定位）、核心持仓表（含持仓观察）、产品表现（净值走势+年度业绩+历史回撤修复）、基金经理观点、上周产品表现（含表现点评）、回撤修复数据、全球市场速览（含A股估值+市场点评）、热门主题表现（含主题描述）、归因分析（带编号颜色）、后市展望（带编号颜色）、基金经理与产品档案、交互式HTML报告附录、Footer风险提示
- **HTML附录页**：在PDF末尾附上一页「HTML在线报告」，包含访问链接 `https://fundadvisor.pages.dev/reports/基金简称_周度回顾.html`

> **样式锁定指令（已固化）**：`fund_weekly_pdf.py` 中所有颜色、字号、边距、间距常量均已硬编码在脚本顶部。LLM执行时**绝对不得修改**模板中的任何样式参数。仅通过修改 `funds_data.json` 数据文件来更新报告内容。数据计算逻辑（如`colored_pct`正红负绿、`gaussian_smooth` sigma=4）已固化，不得调整。

### 3. HTML（红色主调，在线访问，CSS全内联）——**样式已锁定**

> **样式锁定声明**：`generate_htmls.py` 中的 `HTML_TEMPLATE` 字符串为**唯一可信源**，所有 CSS 变量、颜色、字号、间距、卡片布局、导航栏交互均已硬编码。本 skill 执行时**只注入数据变量**，不得修改模板中的任何样式定义。如需调整样式，必须直接修改 `generate_htmls.py` 源文件，并在 SKILL.md 中同步更新版本号。

调用本 skill 自有 HTML 模板生成器 `D:\OPC-skill\skills\fund-weekly-review\templates\generate_htmls.py` 生成单页 HTML 文件，部署至 Cloudflare Pages：

**模板特性**：
- **CSS全部内联**：所有样式写在 `<style>` 标签中，不依赖 Tailwind CSS 或任何外部 CDN（除 Chart.js 和 html2canvas 外）
- **配色**：红色主调（标题 #8b0a1a、正红 #dc2626 涨、负绿 #16a34a 跌），与 PDF 视觉统一
- **布局**：sticky 顶部导航（纯文字，无编号）、卡片式模块、右上角工具栏（复制/下载PDF/下载图片）
- **核心持仓卡片**：2行×5列，每张卡片展示：名称→代码→净值→涨跌幅（正红负绿）
- **全球市场卡片**：2行×2列大卡片（CN/HK/US/JP，带国家代码）+ 1行×4列小卡片
- **热门主题卡片**：3行×4列，右上角「上周」/「今日」按钮切换数据
- **日期规则**：
  - 报告日期 = 上周一到上周五（动态计算：`current_monday - 7天` 到 `+4天`）
  - 制作日期 = 生成当天（JS 刷新时自动更新）
- **交互**：一键复制、热门主题切换、天天基金 JSONP 实时净值刷新（每5分钟）
- **底部风险提示**：不含访问地址，只保留基金说明和数据来源
- **模块**：14个模块（一~十四），其中十一~十三为 HTML 独有（企微专属）
- **差异化下载**：PC端自动下载，企微移动端在线预览（已固化）

**文件命名**：`基金简称_周度回顾.html`
**部署路径**：`skills/portfolio-week-companion/site/reports/基金简称_周度回顾.html`
**访问地址**：`https://fundadvisor.pages.dev/reports/基金简称_周度回顾.html`
**部署方式**：Git push 到 GitHub 后，Cloudflare Pages 自动部署

> **PDF 与 HTML 的关系**：
> - PDF 是「正式文档」——用于打印、存档、邮件附件、合规留痕
> - HTML 是「在线附录」——附在 PDF 末尾，扫码或点击即可访问，用于实时数据查看、手机浏览、一键复制话术
> - 两者内容同源，但 HTML 增加实时数据刷新（每5分钟）和交互功能
> - 最佳实践：邮件发送 PDF，正文附带 HTML 链接；客户可点击链接查看实时更新的网页版
> - 客户需要实时数据、手机浏览、一键复制话术 → 选 HTML
> - 理财经理需要打印存档、正式分发、紧凑排版 → 选 PDF
> - 最佳实践：同时生成两种格式，PDF 用于存档，HTML 用于线上分发
> - **企微场景**：首页 `https://fundadvisor.pages.dev` → 点击「查看 HTML」→ 进入报告页 → 点击「下载PDF」→ PC端直接下载 / 手机端在线预览

---

## 五、实时数据获取与多源降级机制

### 5.1 数据源优先级与分工

采用**多源优先级 + 自动降级**机制，任一数据源失效时自动切换至备用源。浏览器端通过 JavaScript 异步请求实现，服务端通过 Python 脚本轮询实现。

| 优先级 | 数据源 | 用途 | 接入方式 | 浏览器端可用性 | 服务端可用性 |
|--------|--------|------|----------|--------------|--------------|
| **P1** | **Wind (万得)** | 主力数据源：ETF净值、板块资金流向、北向资金、融资余额 | Wind API (`WindPy`) | ❌ 跨域 | ✅ Python SDK |
| **P2** | **iFinD (同花顺)** | 行情、板块资金流向、龙虎榜 | iFinD API (`iFinDPy`) | ❌ 跨域 | ✅ Python SDK |
| **P3** | **Tushare Pro** | ETF日线/实时行情、资金流向、龙虎榜 | Tushare Pro API (`tushare`) | ❌ 跨域 | ✅ Python SDK |
| **P4** | **东方财富 Choice** | ETF实时净值、资金流向、融资融券、北向资金 | Choice API / 网页 | ⚠️ 需代理 | ✅ Python SDK |
| **P5** | **AKShare** | ETF实时行情、板块资金、北向资金、融资融券 | `akshare` | ⚠️ 部分可用 | ✅ Python SDK |
| **P6** | **Baostock** | 个股行情、指数成分股 | `baostock` | ❌ 跨域 | ✅ Python SDK |
| **P7** | **天天基金网 (Eastmoney Fund)** | **基金实时净值、估算净值、涨跌幅、规模** | **JSONP API (`fundgz.1234567.com.cn/js/{code}.js`)** | ✅ **JSONP 无跨域** | ✅ Python + `requests` |
| **P8** | **巨潮资讯网 (cninfo)** | 政策公告、监管文件、上市公司公告 | 网页抓取/官方API | ✅ 可直抓 | ✅ Python SDK |
| **P9** | **盈米 (Yingmi)** | 基金净值、组合诊断、资产配置 | MCP SSE `https://stargate.yingmi.com/mcp/sse?apiKey=FHCFwZCh5gQ4AqAdAnuwGw` | ❌ 需代理 | ✅ SSE 流式 |

### 5.2 浏览器端实时数据获取（前端 JS）

对于部署到互联网（pages.dev）的静态 HTML，**浏览器端**通过以下方式获取实时数据：

#### 基金净值（天天基金 JSONP — P7）

```javascript
// 天天基金实时净值 JSONP（无跨域限制，最可靠）
function fetchFundNav(fundCode) {
  return new Promise((resolve) => {
    const script = document.createElement('script');
    const callbackName = '_fundgz_' + Date.now();
    window[callbackName] = (data) => {
      resolve({
        fundCode: data.fundcode,
        name: data.name,
        nav: data.dwjz,           // 单位净值（最新公布）
        navDate: data.jzrq,       // 净值日期
        estimateNav: data.gsz,    // 估算净值（实时）
        estimateChange: data.gszzl, // 估算涨跌幅%
        estimateTime: data.gztime // 估算时间
      });
      delete window[callbackName];
      document.head.removeChild(script);
    };
    script.src = `https://fundgz.1234567.com.cn/js/${fundCode}.js?rt=${Date.now()}`;
    document.head.appendChild(script);
    setTimeout(() => { resolve(null); delete window[callbackName]; }, 5000);
  });
}
```

#### 指数行情（东方财富 API — P4/P7）

```javascript
// 东方财富开放 API（支持 CORS，可直接 fetch）
async function fetchIndexQuote(secid) {
  // secid 格式：1.000300(沪深300), 0.399006(创业板), 116.HSI(恒生)等
  const url = `https://push2.eastmoney.com/api/qt/stock/get?secid=${secid}&fields=f43,f44,f45,f46,f57,f58,f60,f170,f171`;
  const res = await fetch(url);
  const json = await res.json();
  const d = json.data;
  return {
    code: d.f57, name: d.f58,
    price: (d.f43 / 100).toFixed(2),      // 当前价（分→元）
    open: (d.f46 / 100).toFixed(2),
    high: (d.f44 / 100).toFixed(2),
    low: (d.f45 / 100).toFixed(2),
    prevClose: (d.f60 / 100).toFixed(2),
    changePct: (d.f171 / 100).toFixed(2) + '%'  // 涨跌%
  };
}
```

**常用 secid 映射**：
| 指数 | secid |
|------|-------|
| 沪深300 | `1.000300` 或 `0.399300` |
| 上证指数 | `1.000001` |
| 创业板指 | `0.399006` |
| 恒生指数 | `116.HSI`（可能不可用）→ 降级至 `hkHSI`（新浪）或手动输入 |
| 标普500 | 需通过 Yahoo Finance 或后端代理获取 |
| 黄金(COMEX) | 需通过 Yahoo Finance 或后端代理获取 |

#### 美股/黄金（Yahoo Finance — 降级方案）

```javascript
// 通过 CORS 代理或后端服务获取 Yahoo Finance 数据
// 推荐部署 Cloudflare Worker 做数据转发
async function fetchYahooQuote(symbol) {
  // symbol: ^GSPC(标普500), GC=F(COMEX黄金), ^HSI(恒生)
  const proxyUrl = 'https://your-worker.your-subdomain.workers.dev/quote?symbol=' + symbol;
  const res = await fetch(proxyUrl);
  return res.json();
}
```

### 5.3 自动刷新机制（每 5 分钟）

```javascript
// 页面加载后启动定时刷新
const REFRESH_INTERVAL = 5 * 60 * 1000; // 5分钟

async function refreshData() {
  const fundData = await fetchFundNav('025313');
  if (fundData) updateFundDOM(fundData);
  
  const indices = ['1.000300', '0.399006', '1.000001'];
  for (const secid of indices) {
    const quote = await fetchIndexQuote(secid);
    if (quote) updateIndexDOM(secid, quote);
  }
  
  document.getElementById('last-update').textContent = 
    '数据更新：' + new Date().toLocaleString('zh-CN');
}

// 首次加载 + 定时刷新
refreshData();
setInterval(refreshData, REFRESH_INTERVAL);
```

### 5.4 异常降级策略

```javascript
// 封装带降级的数据获取函数
async function fetchWithFallback(fetchers) {
  for (const fetcher of fetchers) {
    try {
      const data = await fetcher();
      if (data && data.price !== undefined) return data;
    } catch (e) { console.warn('Source failed:', e.message); }
  }
  return null; // 全部降级失败
}

// 使用示例：获取恒生指数
const hsi = await fetchWithFallback([
  () => fetchIndexQuote('116.HSI'),      // P4 东财
  () => fetchYahooQuote('^HSI'),         // P4 Yahoo（需代理）
  () => fetchFromCache('hsi')            // P9 本地缓存兜底
]);
```

---

## 数据收集建议

### 主动权益型额外需要：
- 基金最新净值和上周每日净值
- 最新季报披露的前十大重仓股及行业配置
- 同花顺/东方财富的核心持仓个股上周涨跌幅
- 基金经理最新季报/年报中的"投资策略和运作分析"原文
- 基金经理近期公开访谈、路演核心观点
- 基金历史最大回撤及修复时间数据

### 量化型额外需要：
- 基金最新净值和上周每日净值
- 最新季报披露的行业配置（前5大行业即可，因持仓极度分散）
- 基金相对基准的跟踪误差、信息比率
- 近期市场风格数据（大小盘、价值成长、动量反转等因子表现）
- 基金经理关于量化策略、模型迭代的公开阐述
- 基金历史超额收益归因（如有）
- 基金历史最大回撤及修复时间数据

### 通用数据（两种类型均需）：
- 沪深300、创业板指、科创50、恒生指数、标普500上周收盘和涨跌
- 沪深300 PE、创业板指 PE、10Y国债收益率等估值指标
- 市场热门主题ETF上周表现

如果数据缺失导致某些分析无法进行，应在报告中诚实说明，不虚构内容。

## 注意事项

- 不要机械复制模板中的示例文字，每个判断必须基于实际数据
- 删除不适配的模板痕迹（如"红色=上涨，绿色=下跌"等交互占位符）
- 标题下方只放单个日期，不要重复产品名称，不要放日期范围
- 避免每个新闻板块都用固定四段式（影响/历史/客群/话术），改为直接叙述
- 基金经理观点必须引用实际季报/访谈原文，不编造
- **主动权益与量化分析框架不混淆**：主动权益侧重量化个股，量化侧重量化因子；不强行用个股分析框架套量化产品，也不强行用因子分析框架套主动权益产品
- 数据截止统一使用 `data_cutoff` 字段，封面显示「报告日期 {period_start} 至 {period_end} | 制作日期 {report_date}」
- **首页跳转逻辑已锁定**：`index.html` 中企微移动端必须使用 `window.location.href` 跳转，禁止使用 `window.open()`（会被企微拦截）
- **PDF下载策略已锁定**：HTML模板中 `<a href="xxx.pdf">` 不加 `download` 属性，由页面加载后JS根据UA自动判断（PC端添加download，移动端不添加）
- **报告链接必须带 .html 后缀**：分享链接格式为 `https://fundadvisor.pages.dev/reports/基金简称_周度回顾.html`，不带后缀会触发Cloudflare Pages目录页，在企微中无法点击
- **样式锁定**：所有模板文件的样式常量已硬编码，仅通过修改 `funds_data.json` 更新数据。如需修改样式，必须直接编辑模板源文件，禁止通过LLM生成时修改样式
- **数据完整性**：生成后必须检查所有字段（成立以来、今年以来、近一年、机构持仓、回撤），缺失的用近似数据补充，严禁空白。PDF/HTML中不得出现`+0.00%`或`--`占位符（除基准收益率列外）
- **日期语义**：`update_date` 是报告制作/更新日期（当天），`data_cutoff` 是数据截止日期（上周五），两者不得混淆。index.html列表中显示`更新日期：{update_date}`
- **错别字检查**：HTML生成后检查"标晨500"等常见错别字，修正为"标普500"
- **模板数据驱动**：`generate_htmls.py` 中 sec6~sec13 的内容必须数据驱动注入（通过 `manager_views`、`weekly_analysis`、`attributions`、`outlooks`、`advice`、`news`、`comm_tips` 等字段），严禁在模板中硬编码单只基金的专属文案

---

## 版本变更记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v3.1 | 2026-07-17 | `generate_htmls.py` 去硬编码：sec6~sec13 改为数据变量注入，每支基金自动生成对应基金经理观点、归因分析、话术模板；`funds_data.json` 数据结构明确为字典格式（顶层key=基金代码）；修复10只基金HTML缺少模块的问题 |
| v3.0 | 2026-07-10 | 固化 PDF/HTML 模板样式，所有常量硬编码；新增 HTML 14 模块结构（含 sec11~sec13 企微专属模块）；差异化下载策略（PC端下载/移动端预览） |
| v2.0 | 2026-06-20 | 新增量化型分析框架，支持主动权益与量化双框架切换 |
| v1.0 | 2026-06-10 | 初始版本，支持 Markdown + PDF 输出 |
- **数据完整性**：生成后必须检查所有字段（成立以来、今年以来、近一年、机构持仓、回撤），缺失的用近似数据补充，严禁空白。PDF/HTML中不得出现`+0.00%`或`--`占位符（除基准收益率列外）
- **日期语义**：`update_date` 是报告制作/更新日期（当天），`data_cutoff` 是数据截止日期（上周五），两者不得混淆。index.html列表中显示`更新日期：{update_date}`
- **错别字检查**：HTML生成后检查"标晨500"等常见错别字，修正为"标普500"
